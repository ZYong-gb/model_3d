# note

## 
- AdelaiDepth-main 生成锥形图
- 使用 Depth-Anything-V2 生成的深度图，生成的3d点云为锥形图，锥底为正面，且镜像翻转，且易为平面
- Moge 从单目开放域图像中恢复出三维几何信息，包括测距点云、测距深度图、法线图以及相机的视场角等信息，效果好,特别是近距离（室内），使用键盘建模效果 nice，速度慢
    1. 但是配准时：效果不行

## 解决思路
- 一定要配准吗：
    1. 能不能就看一个面的点云
    2. 能不能使用全景图片进行建模结果，从而解决多张rgb图片的配准结果（点云范围）
    3. 使用远景输电线路rgb加点云数据进行训练模型
    4. 使用远距摄像头，在2d图片上检测到物体时，放大图像进行建模

## 现状
1. 室外远景建模结果太稀疏
2. 细小场景建模识别不到
3. 通过单张 RGB 预测的3d点云数据，其深度存在不确定行，因此在做点云数据配准时会产生重影，导致难以配准